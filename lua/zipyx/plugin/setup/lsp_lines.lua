local lsp_lines = require("lsp_lines")

lsp_lines.setup()

-- lsp_lines.setup({
-- Enable/disable the extension
-- enabled = true,

-- Enable/disable the virtual text
-- virtual_text = true,

-- Enable/disable the underline
-- underline = true,

-- Enable/disable the floating window
-- floating_window = true,

-- Enable/disable the diagnostics_conf
-- })
